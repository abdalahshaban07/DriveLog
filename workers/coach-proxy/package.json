{
  "name": "drivelog-coach-proxy",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret": "wrangler secret put GROQ_API_KEY"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
